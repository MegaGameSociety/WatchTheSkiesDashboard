require 'rails_helper'

RSpec.describe "PublicRelations", :type => :request do
  describe "GET /public_relations" do
    xit "works! (now write some real specs)" do
      get public_relations_path
      expect(response).to have_http_status(200)
    end
  end
end
