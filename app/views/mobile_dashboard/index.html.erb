<div ng-controller="apiCtrl" ng-init="setMobile(true)">
  <div class="country-header">
    <div class="country-items">
      <%= render(:partial => "header") %>
    </div>
    <div ng-if="controlMessage" class="country-messages">{{controlMessage}}</div>
  </div>

  <div class="tabs">
    <div id="tab-news" class="tab news" ng-show="focusedTab === 'news'" ng-controller="newsCtrl">
      <%= render(:partial => "news") %>
    </div>

    <div id="tab-messages" class="tab messages" ng-if="focusedTab === 'messages'" ng-controller="messagesCtrl">
      <%= render(:partial => "messages") %>
    </div>

    <!-- Operatives -->
    <!-- Spying -->

    <div id="tab-income" class="tab income" ng-if="checkPermissions('income') && focusedTab === 'income'" ng-controller="incomeCtrl">
      <%= render(:partial => "income") %>
    </div>


    <!-- Research -->
    <!-- Trades -->
    <!-- Rumours -->
  </div>
</div>
